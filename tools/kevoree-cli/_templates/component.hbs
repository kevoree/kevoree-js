const AbstractComponent = require('kevoree-entities/lib/AbstractComponent');

const {{name}} = AbstractComponent.extend({
  toString: '{{name}}',
  tdef_version: 1,

  start(done) {
    this.log.info('{{name}}', 'Hello world!');
    done();
  },

  stop(done) {
    this.log.info('{{name}}', 'Bye world!');
    done();
  }
});

module.exports = {{name}};
