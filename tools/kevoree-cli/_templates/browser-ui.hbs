const KevoreeModuleLoader = require('kevoree-module-loader');

const pkg = require('../package.json');
const {{name}}UI = require('./browser/{{name}}UI');

KevoreeModuleLoader.register(pkg.name, pkg.version, {{name}}UI);
