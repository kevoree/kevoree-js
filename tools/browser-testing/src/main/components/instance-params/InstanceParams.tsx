import * as React from 'react';

interface UIProps {
  instance: any;
}

export class InstanceParams extends React.Component<UIProps, void> {
  render(): JSX.Element {
    return (
      <div>todo</div>
    );
  }
}
