var async = require('async');

try {
	var bufferutil = require('bufferutil'); // eslint-disable-line
	// TODO do something with bufferutil
} catch (ignore) { /* noop */ }

async.series([], function () {});
